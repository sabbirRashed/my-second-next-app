import React from 'react';

const Header = () => {
    console.log("I am from server")
    return (
        <div>
            <h2>This is the header</h2>
        </div>
    );
};

export default Header;